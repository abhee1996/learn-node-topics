{
  "name": "node-sequelizer-mariadb-app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js && npx sequelize-cli db:migrate",
    "dev": "nodemon app.js && npx sequelize-cli db:migrate",
    "migrate": "npx sequelize-cli db:migrate",
    "init":"npx sequelize-cli init"
  },
  "author": "Muhammad Abdullah",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mariadb": "^3.0.0",
    "morgan": "^1.10.0",
    "mysql2": "^2.3.3",
    "sequelize": "^6.20.1"
  },
  "devDependencies": {
    "sequelize-cli": "^6.4.1"
  }
}
